# Display panes until user selects one
bind q display-panes -d 0

# Start windows at index 1
set -g base-index 1

set -g default-terminal "xterm-256color"

set-option -sa terminal-features ',xterm:RGB'

